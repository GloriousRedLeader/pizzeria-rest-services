# Test Datasource is in memory h2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JWT App settings.
pizza.app.jwt_secret=ErMahGerd123
pizza.app.jwt_expiration_ms=1440000

# Disable redis cache, still need to set some properties to avoid dependency injection errors.
#spring.cache.type=none
spring.redis.host=localhost
spring.redis.port=7999